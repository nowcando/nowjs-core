
/* import { WebFormResponse, WebListResponse, WebStoreResponse } from "../core/base/models/Models";

export class WebUtils {
     public static getFormResponse<T>(data?:T):WebFormResponse<T>{
        return new WebFormResponse(data);
    }

    public static getListResponse<T>(pageindex:number,pageSize:number,totalItems:number,data?:T[]):WebListResponse<T>{
        return new WebListResponse(null,pageindex,pageSize,totalItems,data);
    }

    public static getStoreResponse<T>(fields:any,queryOptions:any,
        pageindex:number,pageSize:number,totalItems:number,data?:T[]):WebStoreResponse<T>{
        return new WebStoreResponse(fields,queryOptions,pageindex,pageSize,totalItems,data);
    }
}

*/
